const rowsCount = 20,
  colsCount = 20,
  cellSize = 40,
  gridColor = "#eaeaea",
  hoveredGridColor = "#9fc9fc",
  fillColor = "#ffffff";

export {
  rowsCount,
  colsCount,
  cellSize,
  gridColor,
  fillColor,
  hoveredGridColor,
};
