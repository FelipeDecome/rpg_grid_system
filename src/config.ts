const rowsCount = 20,
  colsCount = 20,
  cellSize = 40,
  gridColor = "rgba(0,0,0,0.2)",
  gridBorderWidth = 1,
  hoveredGridColor = "#9fc9fc",
  fillColor = "#ffffff";

export {
  rowsCount,
  colsCount,
  cellSize,
  gridColor,
  gridBorderWidth,
  fillColor,
  hoveredGridColor,
};
